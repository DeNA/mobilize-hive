---
development:
  clusters:
    dev_cluster:
      max_slots: 5
      temp_table_db: mobilize
      exec_path: /path/to/hive
test:
  clusters:
    test_cluster:
      max_slots: 5
      temp_table_db: mobilize
      exec_path: /path/to/hive
production:
  clusters:
    prod_cluster:
      max_slots: 5
      temp_table_db: mobilize
      exec_path: /path/to/hive
