---
development:
  prepends:
    - "hive.stats.autogather=false"
  clusters:
    dev_cluster:
      max_slots: 5
      output_db: mobilize
      exec_path: /path/to/hive
test:
  prepends:
    - "hive.stats.autogather=false"
  clusters:
    test_cluster:
      max_slots: 5
      output_db: mobilize
      exec_path: /path/to/hive
production:
  prepends:
    - "hive.stats.autogather=false"
  clusters:
    prod_cluster:
      max_slots: 5
      output_db: mobilize
      exec_path: /path/to/hive
